* Add unit-tests for multi-threaded environment.

* Thoroughly test the use case when the target is released before the observer
  has unregistered from KVO notifications.

* Create a nice sample app for the iOS platform.

* Create a sample app for the Mac OS X platform.
